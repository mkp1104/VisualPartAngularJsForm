@model RegFormAN.Models.INTERVIEWER

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>INTERVIEWER</legend>

        @Html.HiddenFor(model => model.IID)

        <div class="editor-label">
            @Html.LabelFor(model => model.INAME)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.INAME)
            @Html.ValidationMessageFor(model => model.INAME)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SIGN)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SIGN)
            @Html.ValidationMessageFor(model => model.SIGN)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
