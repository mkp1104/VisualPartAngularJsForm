@model IEnumerable<RegFormAN.Models.INTERVIEWER>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.INAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SIGN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.INAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SIGN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IID })
        </td>
    </tr>
}

</table>
